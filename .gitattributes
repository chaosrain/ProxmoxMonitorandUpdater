# Force LF for shell scripts — they are fetched and piped to bash on Linux.
# CRLF would inject \r and break execution.
*.sh   text eol=lf
*.func text eol=lf

# Keep these LF too for consistency
*.md   text eol=lf
LICENSE text eol=lf
.gitignore text eol=lf
.gitattributes text eol=lf
